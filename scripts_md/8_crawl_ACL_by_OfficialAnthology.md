# 切换到根目录
```bash
cd ~
```


# ACL Long Papers (2024, 2025)

## ACL (2025) 全量入库
```bash
# long paper
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2025 \
  --subtype long \
  --delay-seconds 0.5
```

```bash
# short
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2025 \
  --subtype short \
  --delay-seconds 0.5
```

```bash
# findings
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2025 \
  --subtype findings \
  --delay-seconds 0.5
```

## ACL (2024) 全量入库
```bash
# long papers
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2024 \
  --subtype long \
  --delay-seconds 0.5
```

```bash
# short papers
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2024 \
  --subtype short \
  --delay-seconds 0.5
```

```bash
# findings
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2024 \
  --subtype findings \
  --delay-seconds 0.5
```


## ACL (2023) 全量入库
```bash
# long papers
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2023 \
  --subtype long \
  --delay-seconds 0.5
```

```bash
# short papers
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2023 \
  --subtype short \
  --delay-seconds 0.5
```

```bash
# findings
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2023 \
  --subtype findings \
  --delay-seconds 0.5
```

## ACL (2022) 全量入库
```bash
# long papers
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2022 \
  --subtype long \
  --delay-seconds 0.5
```

```bash
# short papers
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2022 \
  --subtype short \
  --delay-seconds 0.5
```

```bash
# findings
python -m ai4research.data_pipeline.scripts_py.crawl_acl_anthology \
  --venue ACL \
  --year 2022 \
  --subtype findings \
  --delay-seconds 0.5
```